import { Instrument_Sans, Inter } from "next/font/google";

export const fontSans = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const fontHeading = Instrument_Sans({
  subsets: ["latin"],
  variable: "--font-heading",
});
